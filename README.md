# hospitalclinicsystem

C++ Win32 desktop GUI recreation of the Honda Clinic hospital management system.

Open `hospitalclinicsystem.sln` in Visual Studio with the Desktop development with C++ workload installed.

The GUI is implemented directly in `main.cpp`; no HTML/CSS or database is required for the C++ application.
